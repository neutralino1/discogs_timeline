{% load url from future %}
<div id="signup-login-boxes" class="center-div">
  <div class="fleft login-box">
    <h2 class="maiden">Sign Up</h2>
    <div class="aright">
      {% if error_message %}<p><strong>{{ error_message }}</strong></p>{% endif %}
      <form action="/users/create/" method="post">
	{% csrf_token %}
	<input type="text" name="email" class="email faded big" value="your@email.com" />
	<!--<label for="email">Your email</label><br />-->
	<input type="password" name="password" class="password faded big" value="password" />
	<input type="text" name="discogs_id" id="discogs_id"
	value="discogs_id" class="faded big"/>
	<!--<label for="password">Password</label><br />-->
	<input type="submit" value="Sign up" class="big" />
      </form>
    </div>
  </div>
  <div class="fright login-box">
    <h2 class="maiden">Login</h2>
    <div class="aright">
      {% if error_message %}<p><strong>{{ error_message }}</strong></p>{% endif %}
      <form action="{% url 'django.contrib.auth.views.login' %}" method="post">
	{% csrf_token %}
	<input type="hidden" name="next" value="{% url 'timeline.views.home' %}" />
	<input type="text" name="username" class="email faded big" value="your@email.com" />
	<!--<label for="email">Your email</label><br />-->
	<input type="password" name="password" class="password faded big" value="password" />
	<!--<label for="password">Password</label><br />-->
	<input type="submit" value="Log in" class="big"/>
      </form>
    </div>
  </div>
</div>

